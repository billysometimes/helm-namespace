apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "namespacechart.fullname" . }}-test-connection"
  labels:
    {{- include "namespacechart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "namespacechart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
